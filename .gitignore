counter.log
*.bak
sample_*.jpg
__pycache__/
*.pyc
.vercel

# sharded runner outputs (scale testing)
counts_shard*.csv
counts_shard*.json
runner.log
runner.out
runner.err
.runner.lock

.env
.env.*

# staging dir for rolled manifest archives — these go to the crops release,
# never into git (rolling them out of git is the entire point)
rotate_out/
